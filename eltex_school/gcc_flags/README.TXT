Learing gcc
Analysis of happening by step:
1. Preprocessing of macrocommands (flag -E);
2. Translation of got pure C to as86 (by default on Linux) (flag -S);
3. Converting as86 to object module (flag -c);
4. Converting object module to execution file (without flag).

